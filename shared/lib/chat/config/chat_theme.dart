import 'package:flutter/material.dart';

/// Shared chat color tokens (Member blue, Trainer red).
abstract class ChatTheme {
  static const memberBlue = Color(0xFF1769E0);
  static const trainerRed = Color(0xFFE50914);
  static const surface = Color(0xFFF9FAFB);
  static const onSurface = Color(0xFF101828);
  static const subtle = Color(0xFF667085);
}
